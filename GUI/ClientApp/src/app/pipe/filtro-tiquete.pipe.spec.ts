import { FiltroTiquetePipe } from './filtro-tiquete.pipe';

describe('FiltroTiquetePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTiquetePipe();
    expect(pipe).toBeTruthy();
  });
});
